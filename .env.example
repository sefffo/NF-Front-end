# Copy this file to .env.local and adjust values for your local setup.

# Backend base URL.
# - Leave empty to use the Vite dev proxy (recommended for local development).
# - Set to your deployed backend URL for staging/production builds.
# VITE_API_BASE_URL=http://localhost:5000/api
